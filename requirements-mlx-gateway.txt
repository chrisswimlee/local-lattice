# DEPRECATED — use `pip install middle-layer[mlx,anthropic]` instead.
#
# This file is a thin compatibility shim kept for one minor version so
# existing tooling that runs `pip install -r requirements-mlx-gateway.txt`
# continues to work. It installs the project in editable mode with both
# the `[mlx]` and `[anthropic]` extras, matching the previous
# everything-but-LM-Studio footprint. See `pyproject.toml`.
#
# Will be removed in 0.2.0.

--editable .[mlx,anthropic]
